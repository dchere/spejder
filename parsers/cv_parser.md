# spejder.cv_parser

**Purpose:**
Handles loading and extracting text from CV documents (PDF, TXT, MD, etc.).

**API:**
- `_load_cv_text`
- `_extract_user_skills_from_cv`

**Context:**
Extracted from `workflows.py` to isolate CV processing logic from workflow orchestration.