# spejder.parsers.email_parser

**Purpose:**
Handles loading and parsing of email/HTML documents (.eml, .html, .htm).

**API:**
- `parse_html_file`
- `load_files`

**Context:**
Originally `spejder.parser`, moved into `parsers/` to consolidate parsing logic.
